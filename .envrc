#!/usr/bin/env bash
# direnv configuration for scripts repository

# Load environment variables from .env file if it exists
if [[ -f .env ]]; then
    dotenv .env
fi

# Add current directory to PATH for easy script execution
PATH_add .