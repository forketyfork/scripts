#!/bin/sh
# Post-commit hook that updates the git index after pre-commit formatting
# Ensures index reflects any formatting changes made during pre-commit

set -e

# Update index to reflect any formatting changes from pre-commit
git update-index -g
